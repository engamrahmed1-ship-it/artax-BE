package com.artax.crm.repository;

import com.artax.crm.entity.CustomerIndividualAddress;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CustomerIndividualAddressRepository extends JpaRepository<CustomerIndividualAddress,Long> {
}
